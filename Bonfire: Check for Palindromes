function palindrome(str) {
  var x = str.replace(/\W*/g,'');
  var y = x.toLowerCase();
  var z = y.split('').reverse().join('');
  if (y === z) {
    return true;
  } else {return false}
}

palindrome("eye");
